[pytest]
testpaths = tests
addopts = --html=reports/report.html --self-contained-html --screenshot only-on-failure -v
markers =
    critical: critical flows tests
    game: game flow tests
    settings: language and theme tests
    auth: account/login tests
    bug_hunting: exploratory bug-hunting tests
